spring:
  application:
    name: media-service
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        namespace: dev
        group: Xingchen-grow-backedn
      config:
        namespace: dev
        group: Xingchen-grow-backedn
        file-extension: yaml
        refresh-enabled: true

#profiles默认为dev
  profiles:
    active: dev



