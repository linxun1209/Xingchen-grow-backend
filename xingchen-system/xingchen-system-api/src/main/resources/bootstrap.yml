#server:
#  servlet:
#    context-path: /system
#  port: 63110
#微服务配置
spring:
  application:
    name: system-api
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        namespace: dev
        group: Xingchen-grow-backend
      config:
        namespace: ${spring.profiles.active}
        group: xuecheng-plus-project
        file-extension: yaml
        refresh-enabled: true
        extension-configs:
          - data-id: content-service-${spring.profiles.active}.yaml
            group: Xingchen-grow-backend
            refresh: true
        shared-configs:
          - data-id: swagger-${spring.profiles.active}.yaml
            group: Xingchen-grow-backend
            refresh: true

# 日志文件配置路径
#logging:
#  config: classpath:log4j2-dev.xml

# swagger 文档配置
swagger:
  title: "星辰成长内容管理系统"
  description: "内容系统管理系统对课程相关信息进行业务管理数据"
  base-package: com.xingchen.system.api
  enabled: true
  version: 1.0.0
